interface:
  display_name: "CommandAgent Issue Split"
  short_description: "Split a large CommandAgent issue safely"
  default_prompt: "Use $issue-split to plan and, when authorized, create focused sub-issues."
policy:
  allow_implicit_invocation: false
