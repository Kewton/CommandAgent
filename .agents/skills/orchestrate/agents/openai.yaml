interface:
  display_name: "CommandAgent Orchestrate"
  short_description: "Run bounded dependency-aware issue orchestration"
  default_prompt: "Use $orchestrate to honor approved dependency and decision inputs while scheduling bounded Issue batches through verified implementation, draft PR, CI, UAT, and guarded merge."
policy:
  allow_implicit_invocation: false
