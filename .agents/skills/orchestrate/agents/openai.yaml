interface:
  display_name: "CommandAgent Orchestrate"
  short_description: "Plan and run CommandAgent issue orchestration"
  default_prompt: "Use $orchestrate to plan CommandAgent issue work before dispatch."
policy:
  allow_implicit_invocation: false
