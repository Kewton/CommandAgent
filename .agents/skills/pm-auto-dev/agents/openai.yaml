interface:
  display_name: "CommandAgent Auto Development"
  short_description: "Run a verified CommandAgent development flow"
  default_prompt: "Use $pm-auto-dev to implement and verify the selected prepared issue."
policy:
  allow_implicit_invocation: false
