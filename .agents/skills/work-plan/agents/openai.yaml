interface:
  display_name: "CommandAgent Work Plan"
  short_description: "Create an issue-scoped CommandAgent work plan"
  default_prompt: "Use $work-plan to create an executable plan for the selected issue."
policy:
  allow_implicit_invocation: false
