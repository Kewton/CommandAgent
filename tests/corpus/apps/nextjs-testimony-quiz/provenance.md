# Fixture provenance

These three fixture files were copied from the real local Quiz-family run
`019fb6a1-a529-7030-a5cb-a8166b587e44` on 2026-07-31. The executor was
`qwen3.6:35b-a3b-coding-nvfp4` and the planner was
`qwen3.6:27b-coding-nvfp4`. The run completed with pre-T1 `full`.

The README and both browser evidence files are measured originals. Only
absolute user-home path components in `browser-interaction.json` were scrubbed
to `/Users/<user>`; no behavioral fields were changed. The measured evidence
contains matched route/interaction observations, absent score/restart
observations, and prose outside the registered lexicon, so one real fixture
fixes the matched, violation, and unrecognized outcomes without synthetic
claims.
