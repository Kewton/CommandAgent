# CSV Aggregator CLI

This tool reports count, sum, and average for a numerical CSV column.

## Usage

```bash
python cli/main.py --file data/sample.csv --column score
```

## Example

```bash
$ python cli/main.py --file data/sample.csv --column score
Count: 5
Sum: 375.0
Average: 75.0
```
