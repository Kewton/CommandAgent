data-inspection=Inspect the workspace CSV or TSV input for: {goal}. Write output/inspection.json with column names, input row count, type summaries, distinct observed values for every categorical column (for example the actual region values), and sample rows. Always write output/inspection.json in this literal shape: {"column_names": ["date","region","amount"], "input_row_count": 60, "type_summaries": {"date":"string(YYYY-MM-DD)","region":"string","amount":"number"}, "distinct_values": {"region": ["東京","大阪","名古屋"]}, "sample_rows": [{"date":"2026-01-01","region":"東京","amount":1000}]}. The values shown are examples only: fill every field with actual observed values from the input data, and never copy the example values as fixed data. Derive later validation rules such as valid regions and date formats only from output/inspection.json observations; never invent an unobserved value set. Verify this phase without depending on pipeline/main.py, output/results.json, or output/report.md.
data-cleaning=Implement deterministic cleaning in pipeline/main.py for: {goal}. Read only the inspected workspace data input, account for every excluded row by a non-empty reason, preserve raw input files, and use Python 3 standard-library csv/json/statistics only. Derive validation rules from output/inspection.json observations and never invent unobserved regions, date formats, or category sets.
data-aggregation=Compute the requested aggregates for: {goal}. Write output/results.json exactly as: {"reconciliation":{"input_rows":N,"used_rows":N,"excluded":[{"reason":"...","rows":N}]},"values":{"<claim_key>":number}}. Store every numeric claim printed in output/report.md under values. Keep all calculation and reconciliation logic in pipeline/main.py.
data-reporting=Generate the required output/report.md from output/results.json; output/report.html is an optional additional artifact. output/results.json must remain exactly: {"reconciliation":{"input_rows":N,"used_rows":N,"excluded":[{"reason":"...","rows":N}]},"values":{"<claim_key>":number}}. Store every numeric claim printed in the report under values and interpolate it from those values; never write an independent numeric literal in report prose.
data-validation=Run only the final-bound pipeline, results-schema, reconciliation, claims-binding, and rerun-consistency checks against the canonical artifacts. data_inspection_schema belongs only to data-inspection and must not be carried into final acceptance. Do not claim full assurance unless every final-bound E1-E4 check passes from observed artifacts.
guidance.generic=Build one reproducible tabular-data pipeline with Python 3 standard-library csv/json/statistics only. Preserve input files. The canonical artifacts are pipeline/main.py, output/inspection.json, output/results.json, and required output/report.md; output/report.html is optional in addition to report.md.
guidance.inspection=When data_inspection_schema fails, write output/inspection.json in this literal shape: {"column_names": ["date","region","amount"], "input_row_count": 60, "type_summaries": {"date":"string(YYYY-MM-DD)","region":"string","amount":"number"}, "distinct_values": {"region": ["東京","大阪","名古屋"]}, "sample_rows": [{"date":"2026-01-01","region":"東京","amount":1000}]}. The values shown are examples only: fill every field with actual observed values from the input data, and never copy the example values as fixed data. Derive validation rules only from output/inspection.json observations. Never invent valid regions, date formats, or category sets that are absent from the inspected data. Inspection verification may read only the input data and output/inspection.json, not later-phase artifacts.
guidance.schema=Write output/results.json exactly as {"reconciliation":{"input_rows":N,"used_rows":N,"excluded":[{"reason":"...","rows":N}]},"values":{"<claim_key>":number}} and store every numeric report claim under values.
guidance.repair=Repair pipeline/main.py and regenerate output/results.json exactly as {"reconciliation":{"input_rows":N,"used_rows":N,"excluded":[{"reason":"...","rows":N}]},"values":{"<claim_key>":number}}. Every numeric claim printed in output/report.md must be present under values. For a claims-binding nearest_miss, report each violation as: "Report claim <claim> has no corresponding results.json value. Nearest candidate <key>=<value>; absolute difference=<difference>." Then export every report numeric claim under an explicit values key, for example regional_名古屋. Never weaken or replace the manifest-bound evidence checks.
required_artifacts=pipeline/main.py,output/inspection.json,output/results.json,output/report.md
template_owned.package_phrases=pipeline/main.py
template_owned.scaffold_phrases=output/inspection.json,output/results.json,output/report.md,output/report.html,data/
template_owned.path_suffixes=pipeline/main.py,output/inspection.json,output/results.json,output/report.md,output/report.html
template_owned.path_contains=data/
