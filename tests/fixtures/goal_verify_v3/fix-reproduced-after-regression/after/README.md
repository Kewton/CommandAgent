# fib-cli

Usage: `python3 cli.py <n>` prints the n-th Fibonacci number.
Exit code 2 on invalid input. Regression set: `python3 -m pytest -q tests`
(id `pytest-tests`) and `python3 scripts/contract_check.py` (id `contract-check`).
