# normalize-lib

Usage: `python3 repro.py` reproduces the full-width digit defect.
Regression set: `python3 -m pytest -q tests` (id `pytest-tests`) and
`python3 scripts/contract_check.py` (id `contract-check`).
