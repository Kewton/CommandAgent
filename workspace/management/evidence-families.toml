# E-5c transverse evidence families. Adding a family is incomplete until the
# Rust wire enum and all three consumer adapter maps pass their guards.
families = ["E", "F", "I", "C", "N", "circle", "workflow"]
