# anvil-recovery-ultra-plan
recovery_schema_version: "1"
recovery_original_goal: "Repair step `implement-inspection-pipeline`. Verification failed: data_inspection_schema:inspection_schema_violation:input_row_count_mismatch:expected=60:reported=24; inspection_schema_violation:distinct_values_missing_categorical_columns:date. Repair target: implementation. Fix the implementation files that should satisfy the requested behavior. Make the smallest bounded change, then stop. Overall goal: data/sales.csv を読み込み、月次の売上合計・前月比（%）・3ヶ月移動平均を計算し、無効な行は理由別に除外して件数を明記した上で、要約レポートを作成してください。 Repa"
recovery_failure_kind: "model_stagnation:read_only_loop"
recovery_profile: "data"
recovery_failed_phase: "data-inspection"
recovery_failed_step: "verify"
goal: "Repair step `implement-inspection-pipeline`. Verification failed: data_inspection_schema:inspection_schema_violation:input_row_count_mismatch:expected=60:reported=24; inspection_schema_violation:distinct_values_missing_categorical_columns:date. Repair target: implementation. Fix the implementation files that should satisfy the requested behavior. Make the smallest bounded change, then stop. Overall goal: data/sales.csv を読み込み、月次の売上合計・前月比（%）・3ヶ月移動平均を計算し、無効な行は理由別に除外して件数を明記した上で、要約レポートを作成してください。 Repa"
profile: "data"
style: "recovery"
intent: "recover"
phases:
  - id: "inspect-current-state"
    prompt: "Inspect the current workspace before changing files. Original goal: Repair step `implement-inspection-pipeline`. Verification failed: data_inspection_schema:inspection_schema_violation:input_row_count_mismatch:expected=60:reported=24; inspection_schema_violation:distinct_values_missing_categorical_columns:date. Repair target: implementation. Fix the implementation files that should satisfy the requested behavior. Make the smallest bounded change, then stop. Overall goal: data/sales.csv を読み込み、月次の売上合計・前月比（%）・3ヶ月移動平均を計算し、無効な行は理由別に除外して件数を明記した上で、要約レポートを作成してください。 Repa. Failed acceptance layer or phase: data-inspection. Failed step: verify. Failure kind: model_stagnation:read_only_loop. Preserve useful existing artifacts and identify the smallest remaining implementation gap."
  - id: "repair-data-inspection"
    prompt: "Repair the incomplete work for the failed phase without restarting from scratch.\nOriginal goal: Repair step `implement-inspection-pipeline`. Verification failed: data_inspection_schema:inspection_schema_violation:input_row_count_mismatch:expected=60:reported=24; inspection_schema_violation:distinct_values_missing_categorical_columns:date. Repair target: implementation. Fix the implementation files that should satisfy the requested behavior. Make the smallest bounded change, then stop. Overall goal: data/sales.csv を読み込み、月次の売上合計・前月比（%）・3ヶ月移動平均を計算し、無効な行は理由別に除外して件数を明記した上で、要約レポートを作成してください。 Repa\nFailed acceptance layer or phase: data-inspection\nFailed step: verify\nMissing capability or artifact signals:\n- repair target: output/inspection.json\nFailure evidence:\n- read_only_stagnation: write_required reached after read_only_streak=6\n- write_required exhausted without Write/Edit to output/inspection.json: attempts=2/2\n- write_required selected_targets=output/inspection.json; selection_reason=required_path\nRepair targets:\n- output/inspection.json\nCreate or update the task-specific implementation artifacts needed to satisfy the original goal. Do not treat scaffold-only, setup-only, style-only, or build-only output as complete."
  - id: "verify-recovery"
    prompt: "Verify the recovered output with deterministic checks and repair only targeted failures.\nOriginal goal: Repair step `implement-inspection-pipeline`. Verification failed: data_inspection_schema:inspection_schema_violation:input_row_count_mismatch:expected=60:reported=24; inspection_schema_violation:distinct_values_missing_categorical_columns:date. Repair target: implementation. Fix the implementation files that should satisfy the requested behavior. Make the smallest bounded change, then stop. Overall goal: data/sales.csv を読み込み、月次の売上合計・前月比（%）・3ヶ月移動平均を計算し、無効な行は理由別に除外して件数を明記した上で、要約レポートを作成してください。 Repa\nFailed acceptance layer or phase: data-inspection\nPreferred verify/browser check:\n- use deterministic file existence, build, route, and capability evidence checks\n- avoid shell control syntax and interactive dev-server-only verification\nVerify preference: use the preferred checks above.\nExpected recovery result: runnable task-specific output, not only a saved plan or diagnostic report."
