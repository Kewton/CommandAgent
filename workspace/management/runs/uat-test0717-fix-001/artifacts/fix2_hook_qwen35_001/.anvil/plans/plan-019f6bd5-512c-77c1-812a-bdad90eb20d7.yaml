goal: "Bind and run one deterministic failing reproducer R before any workspace change for: このNext."
steps:
  - id: "reproduce-missing-restart-hook"
    kind: "verify"
    expected_result: "fail"
    instruction: "Confirm that data-anvil-action=\"restart\" is absent from the application source files to reproduce the contract verification failure."
    expected_paths:
    verify:
      - "grep -r 'data-anvil-action=\"restart\"' src/"
