goal: "Isolate the cause of the reproduced failure without modifying the workspace for: このNext."
steps:
  - id: "inspect-page"
    kind: "inspect"
    expected_result: "pass"
    instruction: "Read src/app/page.tsx to identify interactive UI components and locate where the restart action hook should be implemented."
    expected_paths:
    verify:
  - id: "inspect-layout"
    kind: "inspect"
    expected_result: "pass"
    instruction: "Read src/app/layout.tsx to check for global state or restart affordances."
    expected_paths:
    verify:
  - id: "verify-missing-hook"
    kind: "verify"
    expected_result: "fail"
    instruction: "Confirm that the data-anvil-action=\"restart\" hook is absent from the main page component."
    expected_paths:
    verify:
      - "node -p '(function(s,w,d,q,b){return [new RegExp(\"data-anvil-action\"+w+\"=\"+w+d+\"restart\"+d),new RegExp(\"data-anvil-action\"+w+\"=\"+w+q+\"restart\"+q),new RegExp(\"data-anvil-action\"+w+\"=\"+w+\"[{]\"+w+d+\"restart\"+d+w+\"[}]\"),new RegExp(\"data-anvil-action\"+w+\"=\"+w+\"[{]\"+w+q+\"restart\"+q+w+\"[}]\"),new RegExp(\"data-anvil-action\"+w+\"=\"+w+\"[{]\"+w+b+\"restart\"+b+w+\"[}]\")].some(function(r){return r.test(s)})?true:process.exit(1)})(String(require(\"fs\").readFileSync(\"src/app/page.tsx\")),String.fromCharCode(92)+\"s*\",String.fromCharCode(34),String.fromCharCode(39),String.fromCharCode(96))'"
