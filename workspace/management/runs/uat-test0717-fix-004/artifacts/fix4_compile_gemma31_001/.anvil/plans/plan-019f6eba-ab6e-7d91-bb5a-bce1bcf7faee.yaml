goal: "Bind and run one deterministic failing reproducer R before any workspace change for: このNext."
steps:
  - id: "reproduce-build-failure"
    kind: "verify"
    expected_result: "fail"
    instruction: "Run the Next.js build command to reproduce the reported failure before making any changes."
    expected_paths:
    verify:
      - "npm run build"
