goal: "output/results.json がデータ契約のスキーマ検証に失敗します。原因を調査し、検証可能な再現手順と診断レポート（output/diagnosis.md）を作成してください。修正は行わないでください。"
steps:
  - id: "reproduce-candidate"
    kind: "verify"
    expected_result: "fail"
    instruction: "Execute deterministic reproducer R at stage=diagnosis before diagnosis generation; one reproducer_defect rebuild is permitted before I1 is established."
    expected_paths:
    verify:
      - "anvil-catalog-check:data_results_schema"
