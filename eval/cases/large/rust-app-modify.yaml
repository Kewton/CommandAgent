id: large-rust-app-modify
title: Modify an existing Rust app
profile: rust
style: default
mode: ultra-plan-run
intent: modify
fixture: eval/fixtures/large/rust-app-modify
prompt: "Refactor an existing Rust CLI into modules, add error handling, and keep cargo test passing."
expected_artifacts:
  - Cargo.toml
  - src/main.rs
  - src/lib.rs
verify:
  - cargo test
success_check:
  type: semantic
  required_paths:
    - src/lib.rs
  must_include:
    src/lib.rs:
      - pub
