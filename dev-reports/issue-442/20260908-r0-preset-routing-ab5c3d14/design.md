# Issue #442 corrective design — 2026-09-08

Base: `ab5c3d1461ede2a294a281dc56a5dc82ca97494e`, reached with clean tracked
state, `git fetch origin develop`, then `git merge --ff-only origin/develop`.
Read the original Issue and C-1–C-3 decision, follow-up phase-base/scope-audit,
and the R0 diagnosis, source comparison, and extracted-selector replay limits.

Freeze a new `nextjs-domain-preset-routing` fixture before product edits: the
actual Japanese R0 goal, old/new core tasks derived from exact committed preset
TOML, observed template and required artifacts, with original source hashes.
Keep synthetic lexical/explicit-port controls separate from historical facts.

Move selector-only classification into a Next.js leaf module. Resolve explicit
preset phase intent before keyword fallback: setup and build retain their
templates; core implementation and contract wiring use the planner. For custom
phases retain game exclusions and add general business-implementation intent.
Match ASCII keywords at identifier boundaries, with explicit plural script/
package forms; preserve Japanese keyword matching. TypeScript/import/export
must not provide evidence for port-script optimization. Do not remove any
required type/import/export, response-shape, persistence or domain guidance.

Test real profile preset -> complete production phase prompt (both layouts) ->
registered deterministic template selection. Include the Japanese R0,
English/general business goals, Space/Breakout/Quiz, setup/build and explicit
port-only controls. Assert returned plans/owned artifacts and retain the
existing scaffold/final acceptance and all prior #442 regressions. No copied
selector or template-label stubs serve as the regression test.

Do not change template bodies, required-artifact merge, completion/final gates,
budgets, event names/schema, preflight/verifiers/UI, or other R0 failures. Run
focused regressions first, then full scripts/ci.sh, doc tests, and the pinned
Node/Python domain oracle plus Ruff. Commit task-owned files locally only.

Previous reports and 0907/0908 evidence remain immutable. R0's separate final
failures, nine unstarted business runs and absent B-1 comparison remain limits;
passing routing tests cannot establish live campaign recovery or effects.

Candidate review refinement: phase ids can be reused by edited/custom plans.
Keep the explicit core/wiring fallback, but evaluate phase-local implementation
keywords before selecting setup/build. Global goals/rules remain excluded.
Declare natural plural implementation phrases explicitly; boundary matching
must preserve player controls, stateful updates, user inputs and interactive
surfaces. Freeze reviewer negatives and reproduce both concerns before this
correction. Keep the separately observed full-context default-port selection
outside scope; numeric port assertions use the explicit port-only input while
full-context controls assert template/step selection without claiming that
unrelated port-resolution defect is repaired.

Final bounded choice after review: retain legacy substring matching for the
conservative implementation veto, including its original complete vocabulary.
Apply strict identifier boundaries only to positive template evidence. This
preserves inflections without growing a case-by-case plural list. The veto can
conservatively miss an optimization; it cannot replace implementation work
with a narrower template. Add exact legacy vocabulary provenance and suffix/
scoreboard/controlling controls alongside the four frozen plural negatives.
