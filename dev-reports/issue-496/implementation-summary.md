# Issue #496 implementation

Completed the existing `6085f3a7` capacity-protection implementation on the assigned
feature branch. The saved Next.js proposal retains its entire 2,786-character
instruction and is rejected as ProposalRepairable for capacity before execution.

## Changes

- Admission freezes the actual pre-sanitizer acquisition sources separately from
  the post-transformation preservation view on the first rejection. Both views,
  the untruncated plan, marker obligations and package-script evidence remain
  fixed across later capacity/formation failures and fallback. The separate view
  preserves the existing canonicalization behavior for non-profile steps.
- The pre-augmentation model plan is cloned directly, so duplicate model IDs
  cannot make source acquisition restore the wrong step. Provenance requires one owner ID and the complete augmented instruction before
  sanitization. Missing/changed targets and duplicate IDs authorize no protected
  index. After the sanitizer's notes, host provenance and capacity are checked
  again. Deleted targets remain obligations in the preservation view.
- Marker capture errors remain explicit frozen failures. Neither a later model
  proposal nor changed workspace content can turn unavailable evidence into an
  empty obligation set or acquire a new replacement authorization.
- Admission retry feedback includes the registered contract and formation
  evidence. Schema, lint, quality and empty-response retries carry the same
  frozen baseline through six small driver call sites.
- The corpus now includes the exact host text and verified model/host/tail
  hashes from the referenced campaign, plus negative duty-preservation controls.
  Tests cover capacity boundaries including readiness notes, augmentation
  variants, real Python canonicalization, scaffold/fallback behavior, immutable
  evidence, mixed failures, Recovery and deterministic templates.

## Compatibility and scope

The 2,500 Unicode scalar limit, public model-only truncation, owner/split rules,
verification requirements, event names and event field structure are unchanged.
The existing Issue #478/#484 legal split fixtures remain the independent-owner
positive controls. No four-output split exception or downstream ordering fix was
introduced. The saved fixture is parsed-stage reconstruction, not raw-response
reproduction or evidence about model behavior before parsing.

No live model call, external Issue mutation, PR, push or release was performed.
