# Issue #474 implementation

The preserved compound Node hook command now classifies as StaticSyntax while
remaining unchanged in the final executable registry. The new leaf recognizes
only its complete closed spelling: one literal safe relative source path, all
three ordered includes/throw predicates, and the terminal success print.
General Node Test/throw recognition and existing generated-hook behavior are
unchanged. No business evidence is supplied by the new structural classification.

## Changes and coverage

- Added `recovery_authority/compound_hook.rs` and a small call in the existing
  generated-hook authority. No runner/loop production behavior, gate, schema,
  generation command, registry mutation, or guardrail baseline changed.
- Added a corpus with verbatim saved verify commands, original event-field
  projections with line/hash provenance, a synthetic page, 21 rejected command
  forms, and an independent diagnostic follow-up reproduction.
- Added focused classification, actual execution, final acceptance, registry,
  refresh, and recovery binding tests. Missing primary/input/state produce their
  respective real Node errors. Missing files, alternate paths, stale passes,
  deleted verification steps, shell masks, caught exceptions, uncalled functions,
  display-only/unknown commands, and unrelated assertion files cannot replace the
  registered check or release its failure.
- Preserved genuine Test classification/registration and #439 regressions.

## Repair investigation

The original weak-only report reproduces the two recorded `repair_changed` API
selections with empty selected evidence keys. With the new recognition, a valid
command passes the isolated final boundary; a genuinely failing hook selects its
page via existing `contract_attribute` mapping. Unrelated API edits still fail;
relevant correction plus fresh execution passes. No independent wrong-file
selection defect remains reproduced for this command.

A separate error-summary/attribute-diagnosis defect does remain: primary/input
errors can be summarized away and described as missing state. It is independently
reproduced after recognition and handed to the parent in
[diagnostic-follow-up.md](diagnostic-follow-up.md), as agreed with Codex2. The
failure gate stays closed. This patch does not claim that diagnostic fixed.

## Scope of completion

See [verification.md](verification.md) for worker checks and local release
identity. Parent owns PR/CI, targeted UAT orchestration, merge, and integrated
release version/hash. No model campaign or live capability comparison was run.
Original generated applications, historical evidence, and live runtime namespaces
were not edited. No Issue lifecycle or PR mutation was performed.
