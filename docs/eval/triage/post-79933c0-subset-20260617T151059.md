# Post-79933c0 Subset Eval: 20260617T151059

Date: 2026-06-17

## Scope

This report covers the common-contract recovery work done after `post-831a022-subset-20260617T141228.md`, before returning to the broader recovery plan.

Implemented commits:

- `0a2dad0` - Tighten inspect and verifier plan contracts
- `5cf877e` - Reject mismatched py compile verifiers
- `79933c0` - Split build verification from creation plans

Common fixes included:

- `inspect` steps with no required paths may not use `test -f` / `test -d` as fatal discovery verifiers.
- Source-code semantic `grep -q` remains rejected.
- Shell chaining / fallback verifier commands remain rejected.
- `python -m py_compile` is accepted only for `.py` files.
- Create/edit/setup steps that create missing artifacts may not include build/test/check verifiers; integration checks must be separate `verify` steps.
- Planner and correction prompts now give positive replacement examples.

## Validation

- `cargo test step_runner --lib`: pass
- `cargo test`: pass, 165 tests
- `cargo fmt --check`: pass
- `cargo build --release`: pass

## Final Rerun

- root: `eval/runs/local-recovery-subset/20260617T151059`
- command: `scripts/eval_large_tasks.sh --runs 1 --out eval/runs/local-recovery-subset --model qwen3.6:27b-coding-nvfp4 --timeout-secs 1800 --cases-dir /private/tmp/commandagent-lr4-subset-cases`
- provider: `ollama`
- model: `qwen3.6:27b-coding-nvfp4`
- binary: `target/release/commandagent`
- commit: `79933c0d28b6cca2ea7a0fad3913bda5cbf918d6`
- dirty: `false`

## Result

| Case | Result | Direct Cause | What Changed |
| --- | --- | --- | --- |
| `large-nextjs-app-modify` | `rc=1`, `success=false`, `402948ms` | `npm run build` reached `dependency_missing`; repair then stopped on final-answer contract (`Let me check...`) | Required artifacts were created, `.next` artifacts were produced, and the earlier optional inspect fatal path is gone. Remaining issue is dependency/install policy plus repair no-tool behavior. |
| `large-rust-app-modify` | `rc=1`, `success=false`, `283611ms` | invalid source-code verifier remained after correction: `grep -q "pub fn" src/cli.rs` | The run created `src/lib.rs` and moved beyond the previous missing-artifact state. Remaining issue is planner correction failing to remove source-grep verifiers consistently. |
| `large-rust-app-new` | `rc=1`, `success=false`, `1594507ms` | invalid `expected_result: available` in a late integration-test phase | The run progressed through multiple phases, generated `Cargo.toml`, `src/main.rs`, Cargo.lock/target artifacts, and reached late test planning. The earlier `python -m py_compile src/main.rs` failure is gone. |

Overall headline remains `0/3`, but the failure surface moved materially forward: missing artifacts and wrong-language verifier failures are no longer the main blockers in this final root.

## Comparison Across Reruns

| Root | Commit | Summary | Main Failure Family |
| --- | --- | --- | --- |
| `20260617T141228` | `831a022` | `0/3` | optional inspect fatal, invalid verifier plans, source grep |
| `20260617T144549` | `0a2dad0` | `0/3` | dependency_missing, Rust module duplication, wrong-language `py_compile` |
| `20260617T150112` | `5cf877e` | `0/3` | create+build verifier mixing, persistent source grep, invalid expected_result late |
| `20260617T151059` | `79933c0` | `0/3` | dependency_missing/repair no-tool, persistent source grep after correction, invalid expected_result alias |

## What Improved

1. Optional inspect no longer blocks the first phase.
   - The Next.js modify case moved from `test -d components` fatal failure to creating `components/AnalyticsPanel.tsx` and reaching `npm run build`.

2. Required artifact timing is now behaving better.
   - Rust modify generated `src/lib.rs` in the final root.
   - Rust new generated the required `Cargo.toml` and `src/main.rs` and ran Cargo enough to create `Cargo.lock` and `target/` artifacts.

3. Wrong-language verifier lint worked.
   - The earlier `python -m py_compile src/main.rs` failure did not recur in the final root.

4. Build/test verifier separation is active.
   - Plans that put integration verification directly on creation steps are now rejected/corrected earlier.

## Remaining Issues

### 1. Planner correction is still too weak for source-grep removal

Evidence:

```text
large-rust-app-modify:
plan lint failed: step `create-cli-module` has invalid verifier command `grep -q "pub fn" src/cli.rs`
```

The lint is correct, but the one-shot correction did not reliably convert the invalid plan into build/test/check-based verification. This remains common planner/correction behavior, not Rust profile logic yet.

### 2. Late invalid expected_result alias

Evidence:

```text
large-rust-app-new:
ERROR: invalid step.expected_result: available
```

The model used `available` where the DSL accepts `pass|fail|unavailable`. This is common DSL tolerance / correction behavior. It is not profile-specific.

### 3. Dependency missing remains an eval/profile environment boundary

Evidence:

```text
dependency_missing: verifier_unavailable: npm run build requires node_modules/.bin/next, but it is missing.
```

The Next.js app reached build verification and produced `.next` artifacts, but the repair turn then attempted observation in natural language and hit the final-answer contract. This is no longer an artifact-contract issue. It sits at the boundary of eval fixture dependency availability, Next.js profile setup policy, and repair behavior.

### 4. Repair no-tool after clear diagnostic remains

Evidence:

```text
assistant violated final answer contract: Now let me check if there's a `next.config.js` or `tsconfig.json` file.
```

This is a repair execution issue. It should not be addressed by expanding planner rules further unless it remains after the DSL issues are resolved.

## Classification

| Residual | Layer | Scope | Recommended Bucket |
| --- | --- | --- | --- |
| source-grep persists after correction | planner correction | common | continue common correction refinement before R5 |
| `expected_result: available` | DSL parser/correction | common | accept alias or add correction guidance |
| Next dependency_missing | verifier/profile/eval env | mixed | likely R5 after common residuals |
| repair says “let me check” without tool | executor repair | repair behavior | R6 candidate after common residuals |

## Decision

The latest work followed the intended design direction: it removed ambiguity and strengthened deterministic contracts rather than adding broader autonomous repair behavior.

However, do not fully return to R5/R6 yet. One more narrow common pass is justified:

1. Treat `expected_result: available` as either an alias for `unavailable` or reject/correct it earlier with a clearer message.
2. Strengthen the correction prompt/test path specifically for source-grep verifier removal after lint failure.

After that, rerun the same subset. If remaining failures are `dependency_missing` and repair no-tool behavior, then move back to the original recovery plan:

- R5 for profile/eval environment contracts.
- R6 for repair observability / no-tool repair behavior.
